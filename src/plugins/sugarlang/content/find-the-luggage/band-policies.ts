/**
 * Find the Luggage — Band Policy Defaults.
 *
 * Shared band-level policy defaults for support-language, grounding,
 * response modes, correction posture, and delivery budgets.
 */

import { createDefaultBandPolicyPack } from '../band-policy-defaults';

export const BAND_POLICIES = createDefaultBandPolicyPack();
